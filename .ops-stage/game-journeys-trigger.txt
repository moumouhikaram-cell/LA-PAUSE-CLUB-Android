Apply dedicated end-to-end journeys for every supported game/resource type.
Canonical scope: CONSOLE, SIM_RACING, PC_GAMING, BILLIARD_TABLE, SNOOKER_TABLE, TABLE_TENNIS, PRIVATE_ROOM, CUSTOM.
Variable-amount session coherence fix for PC budget and CUSTOM_AMOUNT is active in the loaded operator action layer.
Harness must use the real entry control of each resource, including CUSTOM amount entry.
FINAL PASS: enforce type-aware history semantics and verify payment -> finish -> paid history for every journey before signed build.
