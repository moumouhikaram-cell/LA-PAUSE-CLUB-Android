name: LA PAUSE OS Legacy Core Audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-core-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Archived internal audit
        run: |
          echo "The former visible-development UI gate is retired."
          echo "Customer releases are validated by client-product-ci.yml."
