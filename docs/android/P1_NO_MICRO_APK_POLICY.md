# P1 release policy

During Phase 1, source commits are development checkpoints only. Do not ship a signed APK for each small change.

The next user-facing APK is gated on a substantial Venue OS Core slice: universal resources + rate plans + sessions + payments/cash + key customer/stock/booking flows + offline persistence/regression checks.
